#!/bin/bash

#1 create project based on Makefile
## create src, build, test folders, with files in src
## files with initial commands to build the project in the makefile file

#2 generate systemd service file simple example (POSTPONED)
